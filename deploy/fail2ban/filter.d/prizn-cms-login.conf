# Fail2ban filter for Prizni CMS login brute-force.
# Matches host Nginx combined access.log (see deploy/nginx/stage2.conf).
#
# Copy to /etc/fail2ban/filter.d/prizn-cms-login.conf

[Definition]
failregex = ^<HOST> - .* "POST /api/auth/login(?:\?[\S]*)? HTTP/[0-9.]+" 401
ignoreregex =
