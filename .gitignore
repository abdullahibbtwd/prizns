# Dependencies
node_modules/

# Builds
dist/
coverage/
*.tsbuildinfo

# Env (shared at root — never commit secrets)
.env
.env.local
.env.*.local
!.env.example

# Secrets / credentials (never commit)
secrets/
scret/
**/secrets/
**/scret/
**/google-service-account.json

# Local phase notes (not for repo)
PHASE*.md
PHASE_*.md

# Logs / OS
logs/
*.log
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/*
!.vscode/extensions.json

# Prisma
api/generated/

# WordPress migration package (local export → VPS import)
wordpress-export/

